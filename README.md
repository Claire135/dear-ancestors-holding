# dearancestors-holding

Static holding page for www.dearancestors.com, served by GitHub Pages.

- `index.html` — the page
- `CNAME` — custom domain binding (do not delete; Pages rewrites it on settings change)
- `.nojekyll` — skip Jekyll processing
